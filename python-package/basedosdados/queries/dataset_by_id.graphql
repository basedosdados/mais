query ($dataset_id: String!, $table_id: String) {
  allDataset(slug: $dataset_id) {
    edges {
      node {
        id
        namePt
        tables (slug: $table_id) {
          edges {
            node {
              id
            }
          }
        }
      }
    }
  }
}