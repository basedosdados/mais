source_bucket_name: basedosdadosdev
project_id_staging: basedosdados-312117
project_id_prod: basedosdados-312117
table_id: indicadores # AUTO GENERATED
dataset_id: br_rj_rio_de_janeiro_ipp_ips # AUTO GENERATED

url_ckan: https://basedosdados.org/dataset/br-rj-rio-de-janeiro-ipp-ips # AUTO GENERATED
url_github: https://github.com/basedosdados/mais/tree/master/bases/br_rj_rio_de_janeiro_ipp_ips # AUTO GENERATED

version: v1.0 # REQUIRED

last_updated: 2021-06-02 # AUTO GENERATED

# Descreva a tabela. Essas são as primeiras frases que um usuário vai ver.
# Você não precisa ser muito conciso. Sinta-se a vontade para dar exemplos de
# como usar os dados.
# Se souber, liste também aplicações: pesquisa, apps, etc. que usem os dados.
description: |  # REQUIRED
    Indicadores do Índice de Progresso Social (IPS)

# Quem está completando esse arquivo config?
published_by:
    name: Pedro Castro  # REQUIRED
    code_url: https://github.com/basedosdados/mais/tree/master/bases/br_rj_rio_de_janeiro_ipp_ips/code # REQUIRED
    website: https://github.com/aspeddro
    email: pdesacastro@gmail.com

# Qual organização/departamento/pessoa tratou os dados?
# As vezes há um ponto intermediário entre os dados originais e subir na Base dos Dados.
# Se essa pessoa é você, preencha abaixo com suas informações.
treated_by:
    name: Pedro Castro
    code_url: https://github.com/basedosdados/mais/tree/master/bases/br_rj_rio_de_janeiro_ipp_ips/code
    website: https://github.com/aspeddro
    email: pdesacastro@gmail.com

# Se houve passos de tratamento, limpeza e manipulação de dados, descreva-os aqui.
treatment_description: | 
    Ajuste nos nomes das regiões administrativas. Adição da coluna ano. Renomeação das colunas.

# Com qual frequência a base é atualizada?
# Opções: hora | dia | semana | mes | 1 ano | 2 anos | 5 anos | 10 anos | unico | recorrente
data_update_frequency: 2 anos # REQUIRED 

# Nível da observação (qual é a granularidade de cada linha na tabela)
# Escolha todas as opções necessárias.
# Regras:
#   - minúsculo, sem acento, singular.
#   - em portugues (ou seja, não use os nomes de colunas abaixo)
# Exemplos: pais, estado, municipio, cidade, hora, dia, semana, mes, ano, etc.
observation_level: #REQUIRED
    - regiao administrativa
    - ano

# Quais colunas identificam uma linha unicamente?
# Preencha com os nomes de colunas. Ex: id_municipio, ano.
# Pode ser vazio pois certas tabelas não possuem identificadores.
primary_keys:
    - regiao_administrativa
    - ano

# Qual é a cobertura espacial da tabela?
# Regras:
#   - minúsculo, sem acento, singular
#   - descer até o menor nível administrativo cuja cobertura abaixo seja 'todos'
# Exemplo 1: tabela que cubra todos os municípios nos estados de SP e GO
#   - brasil
#   - SP, GO
# Exemplo 2: tabela que cubra países inteiros na América Latina
#   - brasil, argentina, peru, equador
coverage_geo:
    - distritos

# Qual é a cobertura temporal (em anos) da tabela?
# Opções: ..., 1990, 1991, ..., 1999, 2000, 2001, ..., 2019, 2020, ...
coverage_time:
    - 2016
    - 2018
    - 2020

# Liste as colunas da tabela que representam partições.
# Não esqueça de deletar essas colunas nas tabelas .csv na hora de subir para o BigQuery.
# Isso poupará muito tempo e dinheiro às pessoas utilizando essa tabela.
# Se não houver partições, não modifique abaixo.
partitions: # REQUIRED

# Quais são as colunas? Certifique-se de escrever uma boa descrição, as pessoas vão gostar
# para saber sobre o que é a coluna.
# Adicionar todas as colunas manualmente pode ser bastante cansativo, por isso, quando
# inicializando este arquivo de configuração, você pode apontar a função para uma amostra de dados que
# preencherá automaticamente as colunas.
# Algumas colunas existirão apenas na tabela final, você as construirá em `publish.sql`.
# Para esses, defina is_in_staging como False.
# Além disso, você deve adicionar as colunas de partição aqui e definir is_partition como True.
columns: # REQUIRED
  
    -   
        name: regiao_administrativa
        description: Região Administrativa
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: ano
        description: Ano
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: mortalidade_na_infancia
        description: Mortalidade Infantil
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: baixo_peso_ao_nascer
        description: Baixo Peso ao Nascer
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: mortalidade_materna
        description: Mortalidade Materna
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: internacoes_infantis_por_crise_respiratoria_aguda
        description: Internações Infantis por Crise Respiratória
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: acesso_a_agua_canalizada
        description: Acesso à Água Canalizada
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: acesso_a_esgotamento_sanitario
        description: Acesso a Esgotamento Sanitário
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: acesso_a_banheiro
        description: Acesso a Banheiro
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: populacao_vivendo_em_favelas_nao_urbanizadas
        description: Pessoas Vivendo em Favelas NãoUrbanizadas
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: acesso_a_energia_eletrica
        description: Acesso à Energia Elétrica
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: adensamento_habitacional_excessivo
        description: Adensamento Habitacional Excessivo
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: taxa_de_homicidios
        description: Taxa de Homicídios
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: roubos_de_rua
        description: Roubos de Rua
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: alfabetizacao
        description: Alfabetização
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: qualidade_do_ensino_fundamental_anos_iniciais
        description: Qualidade do Ensino Fundamental, anos iniciais
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: qualidade_do_ensino_fundamental_anos_finais
        description: Qualidade do Ensino Fundamental, anos finais
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: abandono_escolar_no_ensino_medio
        description: Abandono Escolar no Ensino Médio
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: acesso_a_telefone_celular_ou_fixo
        description: Acesso a Telefone Celular ou Fixo
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: acesso_a_internet
        description: Acesso à internet
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: mortalidade_por_doencas_cronicas
        description: Mortalidade por Doenças Crônicas
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: incidencia_de_dengue
        description: Incidência de Dengue
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: mortalidade_por_tuberculose_e_hiv
        description: Mortalidade por Tuberculose e HIV
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: coleta_seletiva_de_lixo
        description: Coleta Seletiva de Lixo
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: degradacao_de_areas_verdes
        description: Degradação de Áreas Verdes
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: mobilidade_urbana
        description: Mobilidade Urbana
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: homicidios_por_acao_policial
        description: Homicídios por Ação Policial
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: tempo_medio_de_deslocamento
        description: Tempo Médio de Deslocamento
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: participacao_politica
        description: Participação Política
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: gravidez_na_adolescencia
        description: Gravidez na Adolescência
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: trabalho_infantil
        description: Trabalho Infantil
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: indice_de_acesso_a_cultura
        description: Índice de Acesso à Cultura
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: violencia_contra_a_mulher
        description: Violência Contra a Mulher
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: homicidios_de_jovens_negros
        description: Homicídios de Jovens Negros
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: vulnerabilidade_familiar
        description: Vulnerabilidade Familiar
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: pessoas_com_ensino_superior
        description: Pessoas com Ensino Superior
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: negros_e_indigenas_com_ensino_superior
        description: <descrição da coluna>
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: frequencia_ao_ensino_superior
        description: <descrição da coluna>
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
