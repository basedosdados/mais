source_bucket_name: basedosdados-dev
project_id_staging: basedosdados-dev
project_id_prod: basedosdados-dev
table_id: escolas # AUTO GENERATED
dataset_id: br_inep_censo_escolar # AUTO GENERATED

url_ckan: https://basedosdados.org/dataset/br-inep-censo-escolar # AUTO GENERATED
url_github: https://github.com/basedosdados/mais/tree/master/bases/br_inep_censo_escolar # AUTO GENERATED

version: v1.0 # REQUIRED

last_updated: 2021-03-18 # AUTO GENERATED

# Descreva a tabela. Essas são as primeiras frases que um usuário vai ver.
# Você não precisa ser muito conciso. Sinta-se a vontade para dar exemplos de
# como usar os dados.
# Se souber, liste também aplicações: pesquisa, apps, etc. que usem os dados.
description: |  # REQUIRED
    A tabela contém dados do censo escolar ao nível de cada escola. Os dados são organizados em mais de 140 variáveis que traçam um perfil completo da escola, caracterizando desde a infraestrutura física disponível, a equipe pedagógica, a infraestrutura pedagógica, marcas do período letivo como seu começo e fim, além de vários identificadores de cada escola.

# Quem está completando esse arquivo config?
published_by:
    name: Matheus Valentim  # REQUIRED
    code_url: https://github.com/basedosdados/mais/tree/master/bases/br_inep_censo_escolar/code # REQUIRED
    website: https://github.com/mavalentim
    email: mavalentim.b@gmail.com

# Qual organização/departamento/pessoa tratou os dados?
# As vezes há um ponto intermediário entre os dados originais e subir na Base dos Dados.
# Se essa pessoa é você, preencha abaixo com suas informações.
treated_by:
    name: Matheus Valentim  # REQUIRED
    code_url: https://github.com/basedosdados/mais/tree/master/bases/br_inep_censo_escolar/code # REQUIRED
    website: https://github.com/mavalentim
    email: mavalentim.b@gmail.com

# Se houve passos de tratamento, limpeza e manipulação de dados, descreva-os aqui.
treatment_description: | 
    O tratamento consistiu em, principalmente, corretamente harmonizar os diferentes anos da tabela: tanto nomeando as variáveis de maneira compatível quanto verificando se as variáveis significavam de fato a mesma coisa. O outro processo relevante suficiente para ser mencionado é o particionamento da base em estados e anos.

# Com qual frequência a base é atualizada?
# Opções: hora | dia | semana | mes | 1 ano | 2 anos | 5 anos | 10 anos | unico | recorrente
data_update_frequency: 1 ano # REQUIRED 

# Nível da observação (qual é a granularidade de cada linha na tabela)
# Escolha todas as opções necessárias.
# Regras:
#   - minúsculo, sem acento, singular.
#   - em portugues (ou seja, não use os nomes de colunas abaixo)
# Exemplos: pais, estado, municipio, cidade, hora, dia, semana, mes, ano, etc.
observation_level: #REQUIRED
    - escola
    - ano

# Quais colunas identificam uma linha unicamente?
# Preencha com os nomes de colunas. Ex: id_municipio, ano.
# Pode ser vazio pois certas tabelas não possuem identificadores.
primary_keys:
    - id_escola
    - ano

# Qual é a cobertura espacial da tabela?
# Regras:
#   - minúsculo, sem acento, singular
#   - descer até o menor nível administrativo cuja cobertura abaixo seja 'todos'
# Exemplo 1: tabela que cubra todos os municípios nos estados de SP e GO
#   - brasil
#   - SP, GO
# Exemplo 2: tabela que cubra países inteiros na América Latina
#   - brasil, argentina, peru, equador
coverage_geo:
    - brasil
# Qual é a cobertura temporal (em anos) da tabela?
# Opções: ..., 1990, 1991, ..., 1999, 2000, 2001, ..., 2019, 2020, ...
coverage_time:
    - 2007
    - 2008
    - 2009
    - 2010
    - 2011
    - 2012
    - 2013
    - 2014
    - 2015
    - 2016
    - 2017
    - 2018
    - 2019
    - 2020

# Liste as colunas da tabela que representam partições.
# Não esqueça de deletar essas colunas nas tabelas .csv na hora de subir para o BigQuery.
# Isso poupará muito tempo e dinheiro às pessoas utilizando essa tabela.
# Se não houver partições, não modifique abaixo.
partitions: # REQUIRED  
    - ano  
    - sigla_uf

# Quais são as colunas? Certifique-se de escrever uma boa descrição, as pessoas vão gostar
# para saber sobre o que é a coluna.
# Adicionar todas as colunas manualmente pode ser bastante cansativo, por isso, quando
# inicializando este arquivo de configuração, você pode apontar a função para uma amostra de dados que
# preencherá automaticamente as colunas.
# Algumas colunas existirão apenas na tabela final, você as construirá em `publish.sql`.
# Para esses, defina is_in_staging como False.
# Além disso, você deve adicionar as colunas de partição aqui e definir is_partition como True.
columns: # REQUIRED
  
    -   
        name: id_escola
        description: ID Escola - INEP
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: nome_escola
        description: Nome da Escola
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: co_orgao_regional
        description: Código do Órgão Regional
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: tp_situacao_funcionamento
        description: Situação de funcionamento da escola
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: dt_ano_letivo_inicio
        description: Data do início do ano letivo da escola
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: dt_ano_letivo_termino
        description: Data do fim do ano letivo da escola
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: id_municipio
        description: ID Município - IBGE 7 Dígitos
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: tp_dependencia
        description: Esfera de dependência administrativa da escola
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: tp_localizacao
        description: Localização da escola
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: tp_categoria_escola_privada
        description: Tipo de escola privada
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_conveniada_pp
        description: A escola é conveniada com poder público
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: tp_convenio_poder_publico
        description: Tipo de convenio com o poder publico
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_mant_escola_privada_emp
        description: A mantenedora da escola privada é uma empresa ou grupo empresarial
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_mant_escola_privada_ong
        description: A mantenedora da escola privada é uma ONG
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_mant_escola_privada_sind
        description: A mantenedora da escola privada é um sindicato ou associação cooperativa
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_mant_escola_privada_sist_s
        description: A mantenedora da escola privada é o sistema S (SENAI, SESI, SESC, entre outros)
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_mant_escola_privada_s_fins
        description: A mantenedora da escola privada é uma entidade sem fins lucrativos
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: tp_regulamentacao
        description: A escola tem regulamentação ou autorização de algum conselho municipal ou federal
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: tp_ocupacao_predio_escolar
        description: O local de funcionamento da escola é um prédio escolar
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_local_func_salas_empresa
        description: O local de funcionamento da escola é um conjunto de salas de uma empresa
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_local_func_socioeducativo
        description: O local de funcionamento da escola é uma unidade de atendimento socioeducativo
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_predio_compartilhado
        description: O local de funcionamento da escola é um prédio compartilhado
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_local_func_unid_prisional
        description: O local de funcionamento da escola é uma unidade prisional
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_local_func_templo_igreja
        description: O local de funcionamento da escola é um templo ou uma igreja
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_local_func_casa_professor
        description: O local de funcionamento da escola é uma casa de um professor
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: tp_ocupacao_galpao
        description: Forma de ocupação do galpão/rancho/paiol/barracão
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_local_func_outros
        description: O local de funcionamento da escola não se encaixa em outras variáveis
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_local_func_salas_outra_esc
        description: O local de funcionamento da escola é em um conjunto de salas de uma outra escola
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_agua_potavel
        description: A água consumida pelos alunos é filtrada
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_agua_rede_publica
        description: O abastecimento de água é pela rede pública
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_agua_poco_artesiano
        description: O abastecimento de água é feito por um poço artesiano
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_agua_cacimba
        description: O abastecimento de água é feito por uma cacimba/uma cisterna/um poço
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_agua_fonte_rio
        description: O abastecimento de água é feito por uma fonte/um rio/um riacho
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_agua_inexistente
        description: O abastecimento de água é inexistente
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_energia_rede_publica
        description: O abastecimento de energia elétrica é feito pela rede pública
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_energia_gerador
        description: O abastecimento de energia elétrica é por gerador
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_energia_outros
        description: O abastecimento de energia é por outras fontes
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_energia_inexistente
        description: O abastecimento de energia é inexistente
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_esgoto_rede_publica
        description: O esgotamento sanitário é feito pela rede pública
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_esgoto_fossa
        description: O esgotamento sanitário é feito por uma fossa
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_esgoto_inexistente
        description: O esgotamento sanitário é inexistente
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_lixo_servico_coleta
        description: O destino do lixo é a coleta periódica
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_lixo_queima
        description: O destino do lixo é a queima
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_lixo_descarta_outra_area
        description: O destino do lixo é ser descartado em uma outra área
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_tratamento_lixo_reciclagem
        description: O destino o lixo é ser reciclado
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_lixo_enterra
        description: O destino do lixo é ser enterrado
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_lixo_outros
        description: O destino do lixo não se encaixa em nenhuma das outras variáveis
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_sala_diretoria
        description: A escola tem sala de diretoria
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_sala_professor
        description: A escola tem sala de professores
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_laboratorio_informatica
        description: A escola tem laboratório de informática
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_laboratorio_ciencias
        description: A escola tem laboratório de ciências
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_sala_atendimento_especial
        description: A escola tem sala de atendimento especial
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_quadra_esportes
        description: A escola possui quadra para prática de esportes
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_quadra_esportes_coberta
        description: A escola possui quadra coberta para prática de esportes
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_quadra_esportes_descoberta
        description: A escola possui quadra descoberta para prática de esportes
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_cozinha
        description: A escola possui cozinha
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_biblioteca
        description: A escola possui biblioteca
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_sala_leitura
        description: A escola possui sala de leitura
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_parque_infantil
        description: A escola possui parque infantil
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_bercario
        description: A escola possui berçário
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_banheiro_fora_predio
        description: A escola possui banheiros fora do prédio
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_banheiro_dentro_predio
        description: A escola possui banheiros dentro do prédio
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_banheiro_ei
        description: A escola possui banheiros adequado à educação infantil
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_banheiro_pne
        description: A escola possui banheiro adequados a pessoas com deficiência ou mobilidade reduzida
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_dependencias_pne
        description: A escola possui dependências adequadas a pessoas com deficiência ou mobilidade reduzida
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_secretaria
        description: A escola possui uma secretaria
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_banheiro_chuveiro
        description: A escola possui banheiro com chuveiro
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_refeitorio
        description: A escola possui um refeitório
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_despensa
        description: A escola possui uma despensa
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_almoxarifado
        description: A escola possui um almoxarifado
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_auditorio
        description: A escola possui um auditório
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_patio_coberto
        description: A escola possui um pátio coberto
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_patio_descoberto
        description: A escola possui um patio descoberto
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_alojam_aluno
        description: A escola possui um alojamento de alunos
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_alojam_professor
        description: A escola possui um alojamento de professores
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_area_verde
        description: A escola possui uma área verde
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_lavanderia
        description: A escola possui uma lavanderia
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_dependencias_outras
        description: A escola possui outras dependências não inclusas nas outras variáveis
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: qt_salas_utilizadas
        description: Número total de salas utilizadas pela escola, considerando dentro e fora do prédio
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: qt_salas_existentes
        description: Número total de salas existentes na escola
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_equip_tv
        description: A escola possui equipamentos de televisão
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: qt_equip_tv
        description: Quantidade de equipamentos de televisão
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_equip_videocassete
        description: A escola possui equipamentos de videocassete
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: qt_equip_videocassete
        description: Quantidade de equipamentos de videocassete
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_equip_dvd
        description: A escola possui equipamentos de dvd
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: qt_equip_dvd
        description: Quantidade de equipamentos de dvd
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_equip_parabolica
        description: A escola possui equipamentos de antena parabólica
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_equip_copiadora
        description: A escola possui equipamentos de copiadoras
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: qt_equip_copiadora
        description: Quantidade de equipamentos de copiadoras
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_equip_retroprojetor
        description: A escola possui equipamentos de retroprojetor
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_equip_impressora
        description: A escola possui equipamentos de impressora
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: qt_equip_impressora
        description: Quantidade de equipamentos de impressora
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_equip_som
        description: A escola possui equipamentos de som
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: qt_equip_som
        description: Quantidade de equipamentos de som
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_equip_multimidia
        description: A escola possui equipamentos multimídia
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: qt_equip_multimidia
        description: Quantidade de equipamentos multimídia
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_equip_fax
        description: A escola possui equipamentos de fax
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_equip_foto
        description: A escola possui equipamentos de fotografia
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: qt_computador
        description: Quantidade de computadores
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: qt_comp_administrativo
        description: Quantidade de computadores administrativos
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: qt_comp_aluno
        description: Quantidade de computadores para alunos
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_internet
        description: A escola possui acesso à internet
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_banda_larga
        description: A escola possui acesso à internet banda larga
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: qt_funcionarios
        description: Quantidade de funcionários da escola
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_alimentacao
        description: A escola fornece alimentação para os alunos
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: tp_aee
        description: A escola fornece AEE, Atendimento Educacional Especial. Valor 0 = não oferece, valor 1 = não exclusivamente, valor 2 = exclusivamente
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: tp_atividade_complementar
        description: A escola promove atividades complementares. Valor 0 = não oferece, valor 1 = oferece não exclusivamente, valor 2 = oferece exclusivamente
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_regular
        description: A escola oferece metodologia de ensino em etapas consecutivas, da creche ao ensino médio
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_comum_creche
        description: A escola oferece creche em classes comuns do ensino regular 
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_comum_pre
        description: A escola oferece pré-escola em classes comuns do ensino regular
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_comum_fund_ai
        description: A escola oferece anos iniciais do ensino fundamental em classes comuns do ensino regular
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_comum_fund_af
        description: A escola oferece anos finais do ensino fundamental em classes comuns do ensino regular
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_comum_medio_medio
        description: A escola oferece ensino médio (médio propedêutico) em classes comuns do ensino regular
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_comum_medio_integrado
        description: A escola oferece curso técnico integrado ao ensino médio em classes comuns do ensino regular
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_comum_medio_normal
        description: A escola oferece ensino médio (normal ou magistério) em classes comuns do ensino regular
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_comum_prof
        description: A escola oferece educação profissional (curso técnico e/ou curso FIC) em classes comuns
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_especial_exclusiva
        description: A escola oferece pelo menos uma classe para alunos com condições especiais, como deficiência ou superdotação
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_esp_exclusiva_creche
        description: A escola oferece creche em classes especiais exclusivas para alunos com condições especiais
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_esp_exclusiva_pre
        description: A escola oferece pré-escola em classes especiais exclusivas do ensino regular
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_esp_exclusiva_fund_ai
        description: A escola oferece os anos iniciais do ensino fundamental em classes especiais 
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_esp_exclusiva_fund_af
        description: A escola oferece anos finais do ensino fundamental em classes especiais
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_esp_exclusiva_medio_medio
        description: A escola oferece ensino médio (médio propedêutico) em classes especiais
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_esp_exclusiva_medio_integr
        description: A escola oferece curso técnico integrado ao ensino médio em classes especiais
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_esp_exclusiva_medio_normal
        description: A escola oferece ensino médio(normal/magistério) em classes especiais 
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_esp_exclusiva_prof
        description: A escola oferece educação profissional - curso técnico concomitante e/ou susequente e/ou curso FIC concomitante em classes especiais
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_esp_exclusiva_eja_fund
        description: A escola oferece metodologia de ensino
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_esp_exclusiva_eja_medio
        description: A escola oferece metodologia de ensino
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_eja
        description: A escola oferece metodologia de ensino
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_comum_eja_fund
        description: A escola oferece metodologia de ensino
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_comum_eja_medio
        description: A escola oferece metodologia de ensino
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_comum_eja_prof
        description: A escola oferece metodologia de ensino
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_fundamental_ciclos
        description: <descrição da coluna>
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: tp_localizacao_diferenciada
        description: <descrição da coluna>
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_material_esp_nao_utiliza
        description: <descrição da coluna>
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_material_esp_quilombola
        description: <descrição da coluna>
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_material_ped_indigena
        description: <descrição da coluna>
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: in_educacao_indigena
        description: <descrição da coluna>
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: co_lingua_indigena
        description: <descrição da coluna>
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: False # Bool [True, False], whether the column is a partition.
  
    -   
        name: ano
        description: <descrição da coluna>
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: True # Bool [True, False], whether the column is a partition.
  
    -   
        name: sigla_uf
        description: <descrição da coluna>
        is_in_staging: True # Bool [True, False], whether the column is in the staging table
        is_partition: True # Bool [True, False], whether the column is a partition.
