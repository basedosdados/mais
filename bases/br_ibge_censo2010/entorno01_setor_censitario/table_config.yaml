table_id: entorno01_setor_censitario # AUTO GENERATED
dataset_id: br_ibge_censo2010 # AUTO GENERATED

last_updated: 2020-11-30 # AUTO GENERATED

# Descreva a tabela. Essas são as primeiras frases que um usuário vai ver.
# Você não precisa ser muito conciso. Sinta-se a vontade para dar exemplos de
# como usar os dados.
# Se souber, liste também aplicações: pesquisa, apps, etc. que usem os dados.
description: |  # REQUIRED
    Informações a respeito do entorno das quadras dos setores censitários

# Qual organização/departamento/pessoa tratou os dados?
# As vezes há um ponto intermediário entre os dados originais e subir na Base dos Dados.
# Se essa pessoa é você, preencha abaixo com suas informações.
treated_by:
    nome: Diego Oliveira
    code_url: https://github.com/basedosdados/mais/tree/master/bases/br_ibge_censo2010/code   # REQUIRED
    website: github.com/d116626
    email: ds.oliveira404@gmail.com


published_by:
    name: Diego Oliveira   # REQUIRED
    code_url: https://github.com/basedosdados/mais/tree/master/bases/br_ibge_censo2010/code   # REQUIRED
    website: github.com/d116626
    email: ds.oliveira404@gmail.com

# Se houve passos de tratamento, limpeza e manipulação de dados, descreva-os aqui.
treatment_description: |
    Padronização do nome das colunas, remoção de textos e logos, unificação das bases

# Com qual frequência a base é atualizada?
# Opções: hora | dia | semana | mês | 1 ano | 2 anos | 5 anos | 10 anos | único | recorrente
data_update_frequency: 10 anos      # REQUIRED 

# Nível da observação: quais colunas identificam uma linha unicamente?
# Escolha todas as opções necessárias.
# Regras: minúsculo, sem acento, singular.
# Opções (exemplos): pais, estado, municipio, cidade, hora, dia, semana, mes, ano, etc.
observation_level:
    - id_setor_censitario
    - sigla_uf

coverage_geo:
    - BR

coverage_time:
    - 2010

partitions: # REQUIRED  
    - sigla_uf

# Quais são as colunas? Certifique-se de escrever uma boa descrição, as pessoas vão gostar
# para saber sobre o que é a coluna.
# Adicionar todas as colunas manualmente pode ser bastante cansativo, por isso, quando
# inicializando este arquivo de configuração, você pode apontar a função para uma amostra de dados que
# preencherá automaticamente as colunas.
# Algumas colunas existirão apenas na tabela final, você as construirá em `publish.sql`.
# Para esses, defina is_in_staging como False.
# Além disso, você deve adicionar as colunas de partição aqui e definir is_partition como True.
columns: # REQUIRED
    - name: id_setor_censitario
      description: ID do setor
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: sigla_uf
      description: Sigla do Estado
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: true       # Bool [True, False], whether the column is a partition.

    - name: v001
      description: Domicílios particulares permanentes
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v002
      description: Domicílios particulares permanentes próprios –  Existe identificação
        do logradouro
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v003
      description: Domicílios particulares permanentes próprios – Não  existe identificação
        do logradouro
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v004
      description: Domicílios particulares permanentes alugados –  Existe identificação
        do logradouro
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v005
      description: Domicílios particulares permanentes alugados – Não  existe identificação
        do logradouro
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v006
      description: Domicílios particulares permanentes cedidos –  Existe identificação
        do logradouro
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v007
      description: Domicílios particulares permanentes cedidos – Não  existe identificação
        do logradouro
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v008
      description: Domicílios particulares permanentes próprios –  Existe iluminação pública
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v009
      description: Domicílios particulares permanentes próprios – Não  existe iluminação
        pública
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v010
      description: Domicílios particulares permanentes alugados –  Existe iluminação pública
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v011
      description: Domicílios particulares permanentes alugados – Não  existe iluminação
        pública
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v012
      description: Domicílios particulares permanentes cedidos –  Existe iluminação pública
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v013
      description: Domicílios particulares permanentes cedidos – Não  existe iluminação
        pública
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v014
      description: Domicílios particulares permanentes próprios –  Existe pavimentação
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v015
      description: Domicílios particulares permanentes próprios – Não  existe pavimentação
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v016
      description: Domicílios particulares permanentes alugados –  Existe pavimentação
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v017
      description: Domicílios particulares permanentes alugados – Não  existe pavimentação
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v018
      description: Domicílios particulares permanentes cedidos –  Existe pavimentação
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v019
      description: Domicílios particulares permanentes cedidos – Não  existe pavimentação
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v020
      description: Domicílios particulares permanentes próprios –  Existe calçada
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v021
      description: Domicílios particulares permanentes próprios – Não  existe calçada
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v022
      description: Domicílios particulares permanentes alugados –  Existe calçada
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v023
      description: Domicílios particulares permanentes alugados – Não  existe calçada
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v024
      description: Domicílios particulares permanentes cedidos –  Existe calçada
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v025
      description: Domicílios particulares permanentes cedidos – Não  existe calçada
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v026
      description: Domicílios particulares permanentes próprios –  Existe meio-fio/guia
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v027
      description: Domicílios particulares permanentes próprios – Não  existe meio-fio/guia
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v028
      description: Domicílios particulares permanentes alugados –  Existe meio-fio/guia
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v029
      description: Domicílios particulares permanentes alugados – Não  existe meio-fio/guia
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v030
      description: Domicílios particulares permanentes cedidos –  Existe meio-fio/guia
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v031
      description: Domicílios particulares permanentes cedidos – Não  existe meio-fio/guia
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v032
      description: Domicílios particulares permanentes próprios –  Existe bueiro/boca-de-lobo
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v033
      description: Domicílios particulares permanentes próprios – Não  existe bueiro/boca-de-lobo
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v034
      description: Domicílios particulares permanentes alugados –  Existe bueiro/boca-de-lobo
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v035
      description: Domicílios particulares permanentes alugados – Não  existe bueiro/boca-de-lobo
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v036
      description: Domicílios particulares permanentes cedidos –  Existe bueiro/boca-de-lobo
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v037
      description: Domicílios particulares permanentes cedidos – Não  existe bueiro/boca-de-lobo
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v038
      description: Domicílios particulares permanentes próprios –  Existe rampa para cadeirante
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v039
      description: Domicílios particulares permanentes próprios – Não  existe rampa para
        cadeirante
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v040
      description: Domicílios particulares permanentes alugados –  Existe rampa para cadeirante
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v041
      description: Domicílios particulares permanentes alugados – Não  existe rampa para
        cadeirante
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v042
      description: Domicílios particulares permanentes cedidos –  Existe rampa para cadeirante
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v043
      description: Domicílios particulares permanentes cedidos – Não  existe rampa para
        cadeirante
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v044
      description: Domicílios particulares permanentes próprios –  Existe arborização
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v045
      description: Domicílios particulares permanentes próprios – Não  existe arborização
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v046
      description: Domicílios particulares permanentes alugados –  Existe arborização
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v047
      description: Domicílios particulares permanentes alugados – Não  existe arborização
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v048
      description: Domicílios particulares permanentes cedidos –  Existe arborização
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v049
      description: Domicílios particulares permanentes cedidos – Não  existe arborização
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v050
      description: Domicílios particulares permanentes próprios –  Existe esgoto a céu
        aberto
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v051
      description: Domicílios particulares permanentes próprios – Não  existe esgoto a
        céu aberto
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v052
      description: Domicílios particulares permanentes alugados –  Existe esgoto a céu
        aberto
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v053
      description: Domicílios particulares permanentes alugados – Não  existe esgoto a
        céu aberto
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v054
      description: Domicílios particulares permanentes cedidos –  Existe esgoto a céu
        aberto
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v055
      description: Domicílios particulares permanentes cedidos – Não  existe esgoto a
        céu aberto
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v056
      description: Domicílios particulares permanentes próprios –  Existe lixo acumulado
        nos logradouros
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v057
      description: Domicílios particulares permanentes próprios – Não  existe lixo acumulado
        nos logradouros
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v058
      description: Domicílios particulares permanentes alugados –  Existe lixo acumulado
        nos logradouros
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v059
      description: Domicílios particulares permanentes alugados – Não  existe lixo acumulado
        nos logradouros
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v060
      description: Domicílios particulares permanentes cedidos –  Existe lixo acumulado
        nos logradouros
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v061
      description: Domicílios particulares permanentes cedidos – Não  existe lixo acumulado
        nos logradouros
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v062
      description: Domicílios particulares permanentes com rede geral  de distribuição
        de água  – Existe identificação do  logradouro
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v063
      description: Domicílios particulares permanentes com rede geral  de distribuição
        de água  – Não existe identificação do logradouro
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v064
      description: Domicílios particulares permanentes com poço ou  nascente na propriedade
        – Existe identificação do  logradouro
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v065
      description: Domicílios particulares permanentes com poço ou  nascente na propriedade
        – Não existe identificação  do logradouro
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v066
      description: Domicílios particulares permanentes com rede geral  de distribuição
        de água  – Existe iluminação  pública
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v067
      description: Domicílios particulares permanentes com rede geral  de distribuição
        de água  – Não existe iluminação  pública
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v068
      description: Domicílios particulares permanentes com poço ou  nascente na propriedade
        – Existe iluminação pública
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v069
      description: Domicílios particulares permanentes com poço ou  nascente na propriedade
        – Não existe iluminação  pública
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v070
      description: Domicílios particulares permanentes com rede geral  de distribuição
        de água  – Existe pavimentação
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v071
      description: Domicílios particulares permanentes com rede geral  de distribuição
        de água  – Não existe pavimentação
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v072
      description: Domicílios particulares permanentes com poço ou  nascente na propriedade
        – Existe pavimentação
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v073
      description: Domicílios particulares permanentes com poço ou  nascente na propriedade
        – Não existe pavimentação
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v074
      description: Domicílios particulares permanentes com rede geral  de distribuição
        de água  – Existe calçada
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v075
      description: Domicílios particulares permanentes com rede geral  de distribuição
        de água  – Não existe calçada
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v076
      description: Domicílios particulares permanentes com poço ou  nascente na propriedade
        – Existe calçada
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v077
      description: Domicílios particulares permanentes com poço ou  nascente na propriedade
        – Não existe calçada
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v078
      description: Domicílios particulares permanentes com rede geral  de distribuição
        de água  – Existe meio-fio/guia
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v079
      description: Domicílios particulares permanentes com rede geral  de distribuição
        de água  – Não existe meio-fio/guia
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v080
      description: Domicílios particulares permanentes com poço ou  nascente na propriedade
        – Existe meio-fio/guia
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v081
      description: Domicílios particulares permanentes com poço ou  nascente na propriedade
        – Não existe meio-fio/guia
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v082
      description: Domicílios particulares permanentes com rede geral  de distribuição
        de água  – Existe bueiro/boca-de- lobo
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v083
      description: Domicílios particulares permanentes com rede geral  de distribuição
        de água  – Não existe bueiro/boca- de-lobo
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v084
      description: Domicílios particulares permanentes com poço ou  nascente na propriedade
        – Existe bueiro/boca-de- lobo
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v085
      description: Domicílios particulares permanentes com poço ou  nascente na propriedade
        – Não existe bueiro/boca- de-lobo
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v086
      description: Domicílios particulares permanentes com rede geral  de distribuição
        de água  – Existe rampa para  cadeirante
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v087
      description: Domicílios particulares permanentes com rede geral  de distribuição
        de água  – Não existe rampa para  cadeirante
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v088
      description: Domicílios particulares permanentes com poço ou  nascente na propriedade
        – Existe rampa para  cadeirante
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v089
      description: Domicílios particulares permanentes com poço ou  nascente na propriedade
        – Não existe rampa para  cadeirante
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v090
      description: Domicílios particulares permanentes com rede geral  de distribuição
        de água  – Existe arborização
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v091
      description: Domicílios particulares permanentes com rede geral  de distribuição
        de água  – Não existe arborização
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v092
      description: Domicílios particulares permanentes com poço ou  nascente na propriedade
        – Existe arborização
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v093
      description: Domicílios particulares permanentes com poço ou  nascente na propriedade
        – Não existe arborização
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v094
      description: Domicílios particulares permanentes com rede geral  de distribuição
        de água  – Existe esgoto a céu  aberto
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v095
      description: Domicílios particulares permanentes com rede geral  de distribuição
        de água  – Não existe esgoto a céu  aberto
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v096
      description: Domicílios particulares permanentes com poço ou  nascente na propriedade
        – Existe esgoto a céu  aberto
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v097
      description: Domicílios particulares permanentes com poço ou  nascente na propriedade
        – Não existe esgoto a céu  aberto
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v098
      description: Domicílios particulares permanentes com rede geral  de distribuição
        de água  – Existe lixo acumulado  nos logradouros
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v099
      description: Domicílios particulares permanentes com rede geral  de distribuição
        de água  – Não existe lixo  acumulado nos logradouros
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v100
      description: Domicílios particulares permanentes com poço ou  nascente na propriedade
        – Existe lixo acumulado nos logradouros
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v101
      description: Domicílios particulares permanentes com poço ou  nascente na propriedade
        – Não existe lixo acumulado nos logradouros
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v102
      description: Domicílios particulares permanentes que tinham  banheiro ou sanitário
        e esgotamento sanitário da  rede geral de esgoto ou rede pluvial – Existe  identificação
        do logradouro
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v103
      description: Domicílios particulares permanentes que tinham  banheiro ou sanitário
        e esgotamento sanitário da  rede geral de esgoto ou rede pluvial – Não existe  identificação
        do logradouro
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v104
      description: Domicílios particulares permanentes que tinham  banheiro ou sanitário
        e esgotamento sanitário de  fossa séptica – Existe identificação do logradouro
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v105
      description: Domicílios particulares permanentes que tinham  banheiro ou sanitário
        e esgotamento sanitário de  fossa séptica – Não existe identificação do  logradouro
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v106
      description: Domicílios particulares permanentes que não tinham  banheiro ou sanitário
        – Existe identificação do  logradouro
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v107
      description: Domicílios particulares permanentes que não tinham  banheiro ou sanitário
        – Não existe identificação do logradouro
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v108
      description: Domicílios particulares permanentes que tinham  banheiro ou sanitário
        e esgotamento sanitário da  rede geral de esgoto ou rede pluvial – Existe  iluminação
        pública
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v109
      description: Domicílios particulares permanentes que tinham  banheiro ou sanitário
        e esgotamento sanitário da  rede geral de esgoto ou rede pluvial – Não existe  iluminação
        pública
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v110
      description: Domicílios particulares permanentes que tinham  banheiro ou sanitário
        e esgotamento sanitário de  fossa séptica – Existe iluminação pública
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v111
      description: Domicílios particulares permanentes que tinham  banheiro ou sanitário
        e esgotamento sanitário de  fossa séptica – Não existe iluminação pública
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v112
      description: Domicílios particulares permanentes que não tinham  banheiro ou sanitário
        – Existe iluminação pública
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v113
      description: Domicílios particulares permanentes que não tinham  banheiro ou sanitário
        – Não existe iluminação  pública
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v114
      description: Domicílios particulares permanentes que tinham  banheiro ou sanitário
        e esgotamento sanitário da  rede geral de esgoto ou rede pluvial – Existe  pavimentação
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v115
      description: Domicílios particulares permanentes que tinham  banheiro ou sanitário
        e esgotamento sanitário da  rede geral de esgoto ou rede pluvial – Não existe  pavimentação
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v116
      description: Domicílios particulares permanentes que tinham  banheiro ou sanitário
        e esgotamento sanitário de  fossa séptica – Existe pavimentação
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v117
      description: Domicílios particulares permanentes que tinham  banheiro ou sanitário
        e esgotamento sanitário de  fossa séptica – Não existe pavimentação
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v118
      description: Domicílios particulares permanentes que não tinham  banheiro ou sanitário
        – Existe pavimentação
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v119
      description: Domicílios particulares permanentes que não tinham  banheiro ou sanitário
        – Não existe pavimentação
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v120
      description: Domicílios particulares permanentes que tinham  banheiro ou sanitário
        e esgotamento sanitário da  rede geral de esgoto ou rede pluvial – Existe  calçada
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v121
      description: Domicílios particulares permanentes que tinham  banheiro ou sanitário
        e esgotamento sanitário da  rede geral de esgoto ou rede pluvial – Não existe  calçada
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v122
      description: Domicílios particulares permanentes que tinham  banheiro ou sanitário
        e esgotamento sanitário de  fossa séptica – Existe calçada
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v123
      description: Domicílios particulares permanentes que tinham  banheiro ou sanitário
        e esgotamento sanitário de  fossa séptica – Não existe calçada
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v124
      description: Domicílios particulares permanentes que não tinham  banheiro ou sanitário
        – Existe calçada
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v125
      description: Domicílios particulares permanentes que não tinham  banheiro ou sanitário
        – Não existe calçada
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v126
      description: Domicílios particulares permanentes que tinham  banheiro ou sanitário
        e esgotamento sanitário da  rede geral de esgoto ou rede pluvial – Existe meio-
        fio/guia
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v127
      description: Domicílios particulares permanentes que tinham  banheiro ou sanitário
        e esgotamento sanitário da  rede geral de esgoto ou rede pluvial – Não existe  meio-fio/guia
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v128
      description: Domicílios particulares permanentes que tinham  banheiro ou sanitário
        e esgotamento sanitário de  fossa séptica – Existe meio-fio/guia
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v129
      description: Domicílios particulares permanentes que tinham  banheiro ou sanitário
        e esgotamento sanitário de  fossa séptica – Não existe meio-fio/guia
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v130
      description: Domicílios particulares permanentes que não tinham  banheiro ou sanitário
        – Existe meio-fio/guia
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v131
      description: Domicílios particulares permanentes que não tinham  banheiro ou sanitário
        – Não existe meio-fio/guia
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v132
      description: Domicílios particulares permanentes que tinham  banheiro ou sanitário
        e esgotamento sanitário da  rede geral de esgoto ou rede pluvial – Existe  bueiro/boca-de-lobo
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v133
      description: Domicílios particulares permanentes que tinham  banheiro ou sanitário
        e esgotamento sanitário da  rede geral de esgoto ou rede pluvial – Não existe  bueiro/boca-de-lobo
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v134
      description: Domicílios particulares permanentes que tinham  banheiro ou sanitário
        e esgotamento sanitário de  fossa séptica – Existe bueiro/boca-de-lobo
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v135
      description: Domicílios particulares permanentes que tinham  banheiro ou sanitário
        e esgotamento sanitário de  fossa séptica – Não existe bueiro/boca-de-lobo
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v136
      description: Domicílios particulares permanentes que não tinham  banheiro ou sanitário
        – Existe bueiro/boca-de-lobo
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v137
      description: Domicílios particulares permanentes que não tinham  banheiro ou sanitário
        – Não existe bueiro/boca-de- lobo
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v138
      description: Domicílios particulares permanentes que tinham  banheiro ou sanitário
        e esgotamento sanitário da  rede geral de esgoto ou rede pluvial – Existe rampa
        para cadeirante
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v139
      description: Domicílios particulares permanentes que tinham  banheiro ou sanitário
        e esgotamento sanitário da  rede geral de esgoto ou rede pluvial – Não existe  rampa
        para cadeirante
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v140
      description: Domicílios particulares permanentes que tinham  banheiro ou sanitário
        e esgotamento sanitário de  fossa séptica – Existe rampa para cadeirante
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v141
      description: Domicílios particulares permanentes que tinham  banheiro ou sanitário
        e esgotamento sanitário de  fossa séptica – Não existe rampa para cadeirante
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v142
      description: Domicílios particulares permanentes que não tinham  banheiro ou sanitário
        – Existe rampa para  cadeirante
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v143
      description: Domicílios particulares permanentes que não tinham  banheiro ou sanitário
        – Não existe rampa para  cadeirante
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v144
      description: Domicílios particulares permanentes que tinham  banheiro ou sanitário
        e esgotamento sanitário da  rede geral de esgoto ou rede pluvial – Existe  arborização
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v145
      description: Domicílios particulares permanentes que tinham  banheiro ou sanitário
        e esgotamento sanitário da  rede geral de esgoto ou rede pluvial – Não existe  arborização
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v146
      description: Domicílios particulares permanentes que tinham  banheiro ou sanitário
        e esgotamento sanitário de  fossa séptica – Existe arborização
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v147
      description: Domicílios particulares permanentes que tinham  banheiro ou sanitário
        e esgotamento sanitário de  fossa séptica – Não existe arborização
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v148
      description: Domicílios particulares permanentes que não tinham  banheiro ou sanitário
        – Existe arborização
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v149
      description: Domicílios particulares permanentes que não tinham  banheiro ou sanitário
        – Não existe arborização
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v150
      description: Domicílios particulares permanentes que tinham  banheiro ou sanitário
        e esgotamento sanitário da  rede geral de esgoto ou rede pluvial – Existe  esgoto
        a céu aberto
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v151
      description: Domicílios particulares permanentes que tinham  banheiro ou sanitário
        e esgotamento sanitário da  rede geral de esgoto ou rede pluvial – Não existe  esgoto
        a céu aberto
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v152
      description: Domicílios particulares permanentes que tinham  banheiro ou sanitário
        e esgotamento sanitário de  fossa séptica – Existe esgoto a céu aberto
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v153
      description: Domicílios particulares permanentes que tinham  banheiro ou sanitário
        e esgotamento sanitário de  fossa séptica – Não existe esgoto a céu aberto
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v154
      description: Domicílios particulares permanentes que não tinham  banheiro ou sanitário
        – Existe esgoto a céu aberto
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v155
      description: Domicílios particulares permanentes que não tinham  banheiro ou sanitário
        – Não existe esgoto a céu  aberto
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v156
      description: Domicílios particulares permanentes que tinham  banheiro ou sanitário
        e esgotamento sanitário da  rede geral de esgoto ou rede pluvial – Existe lixo  acumulado
        nos logradouros
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v157
      description: Domicílios particulares permanentes que tinham  banheiro ou sanitário
        e esgotamento sanitário da  rede geral de esgoto ou rede pluvial – Não existe  lixo
        acumulado nos logradouros
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v158
      description: Domicílios particulares permanentes que tinham  banheiro ou sanitário
        e esgotamento sanitário de  fossa séptica – Existe lixo acumulado nos  logradouros
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v159
      description: Domicílios particulares permanentes que tinham  banheiro ou sanitário
        e esgotamento sanitário de  fossa séptica – Não existe lixo acumulado nos  logradouros
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v160
      description: Domicílios particulares permanentes que não tinham  banheiro ou sanitário
        – Existe lixo acumulado nos  logradouros
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v161
      description: Domicílios particulares permanentes que não tinham  banheiro ou sanitário
        – Não existe lixo acumulado  nos logradouros
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v162
      description: Domicílios particulares permanentes com lixo  coletado diretamente
        por serviço de limpeza –  Existe identificação do logradouro
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v163
      description: Domicílios particulares permanentes com lixo  coletado diretamente
        por serviço de limpeza – Não  existe identificação do logradouro
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v164
      description: Domicílios particulares permanentes com lixo  depositado em caçamba
        de serviço de limpeza –  Existe identificação do logradouro
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v165
      description: Domicílios particulares permanentes com lixo  depositado em caçamba
        de serviço de limpeza – Não  existe identificação do logradouro
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v166
      description: Domicílios particulares permanentes com lixo  coletado diretamente
        por serviço de limpeza –  Existe iluminação pública
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v167
      description: Domicílios particulares permanentes com lixo  coletado diretamente
        por serviço de limpeza – Não  existe iluminação pública
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v168
      description: Domicílios particulares permanentes com lixo  depositado em caçamba
        de serviço de limpeza –  Existe iluminação pública
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v169
      description: Domicílios particulares permanentes com lixo  depositado em caçamba
        de serviço de limpeza – Não  existe iluminação pública
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v170
      description: Domicílios particulares permanentes com lixo  coletado diretamente
        por serviço de limpeza –  Existe pavimentação
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v171
      description: Domicílios particulares permanentes com lixo  coletado diretamente
        por serviço de limpeza – Não  existe pavimentação
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v172
      description: Domicílios particulares permanentes com lixo  depositado em caçamba
        de serviço de limpeza –  Existe pavimentação
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v173
      description: Domicílios particulares permanentes com lixo  depositado em caçamba
        de serviço de limpeza – Não  existe pavimentação
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v174
      description: Domicílios particulares permanentes com lixo  coletado diretamente
        por serviço de limpeza –  Existe calçada
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v175
      description: Domicílios particulares permanentes com lixo  coletado diretamente
        por serviço de limpeza – Não  existe calçada
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v176
      description: Domicílios particulares permanentes com lixo  depositado em caçamba
        de serviço de limpeza –  Existe calçada
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v177
      description: Domicílios particulares permanentes com lixo  depositado em caçamba
        de serviço de limpeza – Não  existe calçada
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v178
      description: Domicílios particulares permanentes com lixo  coletado diretamente
        por serviço de limpeza –  Existe meio-fio/guia
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v179
      description: Domicílios particulares permanentes com lixo  coletado diretamente
        por serviço de limpeza – Não  existe meio-fio/guia
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v180
      description: Domicílios particulares permanentes com lixo  depositado em caçamba
        de serviço de limpeza –  Existe meio-fio/guia
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v181
      description: Domicílios particulares permanentes com lixo  depositado em caçamba
        de serviço de limpeza – Não  existe meio-fio/guia
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v182
      description: Domicílios particulares permanentes com lixo  coletado diretamente
        por serviço de limpeza –  Existe bueiro/boca-de-lobo
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v183
      description: Domicílios particulares permanentes com lixo  coletado diretamente
        por serviço de limpeza – Não  existe bueiro/boca-de-lobo
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v184
      description: Domicílios particulares permanentes com lixo  depositado em caçamba
        de serviço de limpeza –  Existe bueiro/boca-de-lobo
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v185
      description: Domicílios particulares permanentes com lixo  depositado em caçamba
        de serviço de limpeza – Não  existe bueiro/boca-de-lobo
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v186
      description: Domicílios particulares permanentes com lixo  coletado diretamente
        por serviço de limpeza –  Existe rampa para cadeirante
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v187
      description: Domicílios particulares permanentes com lixo  coletado diretamente
        por serviço de limpeza – Não  existe rampa para cadeirante
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v188
      description: Domicílios particulares permanentes com lixo  depositado em caçamba
        de serviço de limpeza –  Existe rampa para cadeirante
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v189
      description: Domicílios particulares permanentes com lixo  depositado em caçamba
        de serviço de limpeza – Não  existe rampa para cadeirante
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v190
      description: Domicílios particulares permanentes com lixo  coletado diretamente
        por serviço de limpeza –  Existe arborização
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v191
      description: Domicílios particulares permanentes com lixo  coletado diretamente
        por serviço de limpeza – Não  existe arborização
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v192
      description: Domicílios particulares permanentes com lixo  depositado em caçamba
        de serviço de limpeza –  Existe arborização
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v193
      description: Domicílios particulares permanentes com lixo  depositado em caçamba
        de serviço de limpeza – Não  existe arborização
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v194
      description: Domicílios particulares permanentes com lixo  coletado diretamente
        por serviço de limpeza –  Existe esgoto a céu aberto
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v195
      description: Domicílios particulares permanentes com lixo  coletado diretamente
        por serviço de limpeza – Não  existe esgoto a céu aberto
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v196
      description: Domicílios particulares permanentes com lixo  depositado em caçamba
        de serviço de limpeza –  Existe esgoto a céu aberto
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v197
      description: Domicílios particulares permanentes com lixo  depositado em caçamba
        de serviço de limpeza – Não  existe esgoto a céu aberto
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v198
      description: Domicílios particulares permanentes com lixo  coletado diretamente
        por serviço de limpeza –  Existe lixo acumulado nos logradouros
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v199
      description: Domicílios particulares permanentes com lixo  coletado diretamente
        por serviço de limpeza – Não  existe lixo acumulado nos logradouros
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v200
      description: Domicílios particulares permanentes com lixo  depositado em caçamba
        de serviço de limpeza –  Existe lixo acumulado nos logradouros
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

    - name: v201
      description: Domicílios particulares permanentes com lixo  depositado em caçamba
        de serviço de limpeza – Não  existe lixo acumulado nos logradouros
      is_in_staging: true       # Bool [True, False], whether the column is in the staging table
      is_partition: false       # Bool [True, False], whether the column is a partition.

