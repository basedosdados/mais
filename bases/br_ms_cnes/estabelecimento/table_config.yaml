source_bucket_name: basedosdados-dev
project_id_staging: basedosdados-dev
project_id_prod: basedosdados-dev
table_id: estabelecimento # AUTO GENERATED
dataset_id: br_ms_cnes # AUTO GENERATED

url_ckan: https://basedosdados.org/dataset/br-ms-cnes # AUTO GENERATED
url_github: https://github.com/basedosdados/mais/tree/master/bases/br_ms_cnes # AUTO GENERATED

version: v1.0 # REQUIRED

last_updated: 2021-09-06 # AUTO GENERATED

# Descreva a tabela. Essas são as primeiras frases que um usuário vai ver.
# Você não precisa ser muito conciso. Sinta-se a vontade para dar exemplos de
# como usar os dados.
# Se souber, liste também aplicações: pesquisa, apps, etc. que usem os dados.
description: |  # REQUIRED
    Tabela que define as informações sobre administração do estabelecimento de saúde e seu funcionamento

# Quem está completando esse arquivo config?
published_by:
    name: Gustavo Aires Tiago  # REQUIRED
    code_url: https://github.com/basedosdados/mais/tree/master/bases/br_ms_cnes/code # REQUIRED
    website: https://basedosdados.org/dataset/br-ms-cnes
    email: gustavo.tiago@basedosdados.org

# Qual organização/departamento/pessoa tratou os dados?
# As vezes há um ponto intermediário entre os dados originais e subir na Base dos Dados.
# Se essa pessoa é você, preencha abaixo com suas informações.
treated_by:
    name: Gustavo Aires Tiago
    code_url: https://github.com/basedosdados/mais/tree/master/bases/br_ms_cnes/code
    website: https://basedosdados.org/dataset/br-ms-cnes
    email: gustavo.tiago@basedosdados.org

# Se houve passos de tratamento, limpeza e manipulação de dados, descreva-os aqui.
treatment_description: |
    - Padronização de colunas
    - Exclusão de caracter '0'
    - Inclusão de identificadores de id_municipio
    - Transformação de datas
    - Exclusão da Linha 4020 de Janeiro de 2006 - Rio de Janeiro por falta de informações

# Com qual frequência a base é atualizada?
# Opções: hora | dia | semana | mes | 1 ano | 2 anos | 5 anos | 10 anos | unico | recorrente
data_update_frequency: mes # REQUIRED 

# Nível da observação (qual é a granularidade de cada linha na tabela)
# Escolha todas as opções necessárias.
# Regras:
#   - minúsculo, sem acento, singular.
#   - em portugues (ou seja, não use os nomes de colunas abaixo)
# Exemplos: pais, estado, municipio, cidade, hora, dia, semana, mes, ano, etc.
observation_level: #REQUIRED
    - estabelecimento

# Quais colunas identificam uma linha unicamente?
# Preencha com os nomes de colunas. Ex: id_municipio, ano.
# Pode ser vazio pois certas tabelas não possuem identificadores.
primary_keys:
    - id_cnes
    - ano
    - mes

# Qual é a cobertura espacial da tabela?
# Regras:
#   - minúsculo, sem acento, singular
#   - descer até o menor nível administrativo cuja cobertura abaixo seja 'todos'
# Exemplo 1: tabela que cubra todos os municípios nos estados de SP e GO
#   - brasil
#   - SP, GO
# Exemplo 2: tabela que cubra países inteiros na América Latina
#   - brasil, argentina, peru, equador
coverage_geo:
    - brasil

# Qual é a cobertura temporal (em anos) da tabela?
# Opções: ..., 1990, 1991, ..., 1999, 2000, 2001, ..., 2019, 2020, ...
coverage_time:
    - 2005
    - 2006
    - 2007
    - 2008
    - 2009
    - 2010
    - 2011
    - 2012
    - 2013
    - 2014
    - 2015
    - 2016
    - 2017
    - 2018
    - 2019
    - 2020
    - 2021

# Liste as colunas da tabela que representam partições.
# Não esqueça de deletar essas colunas nas tabelas .csv na hora de subir para o BigQuery.
# Isso poupará muito tempo e dinheiro às pessoas utilizando essa tabela.
# Se não houver partições, não modifique abaixo.
partitions: # REQUIRED
    - ano
    - mes
    - sigla_uf
# Quais são as colunas? Certifique-se de escrever uma boa descrição, as pessoas vão gostar
# para saber sobre o que é a coluna.
# Adicionar todas as colunas manualmente pode ser bastante cansativo, por isso, quando
# inicializando este arquivo de configuração, você pode apontar a função para uma amostra de dados que
# preencherá automaticamente as colunas.
# Algumas colunas existirão apenas na tabela final, você as construirá em `publish.sql`.
# Para esses, defina is_in_staging como False.
# Além disso, você deve adicionar as colunas de partição aqui e definir is_partition como True.
columns: # REQUIRED
    - name: ano
      description: ano 
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: true   # Bool [True, False], whether the column is a partition.
    - name: mes
      description: mês
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: true   # Bool [True, False], whether the column is a partition.
    - name: sigla_uf
      description: Sigla da Unidade da Federação
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: true   # Bool [True, False], whether the column is a partition.
    - name: id_municipio
      description: ID Município IBGE - 7 Dígitos
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: id_municipio_6
      description: ID Município IBGE - 6 Dígitos
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: id_cnes
      description: Número nacional do estabelecimento de saúde
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: regiao_saude
      description: Código da região de saúde NOAS
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: microrregiao_saude
      description: Código da micro-região de saúde NOAS
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: distrito_sanitario
      description: Código do distrito sanitário
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: distrito_administrativo
      description: Código do Módulo Assistencial
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: cep
      description: Código do CEP do estabelecimento
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: cpf_cnpj
      description: CPF do estabelecimento, caso pessoa física ou CNPJ, caso pessoa jurídica
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: indicador_pessoa
      description: Indicador de pessoa
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: grau_dependencia
      description: Grau de dependência
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: cnpj_mantenedora
      description: CNPJ da mantenedora do estabelecimento.
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: codigo_ir
      description: Indica o tipo de retenção de tributos da mantenedora
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: vinculo_sus
      description: Vínculo com SUS
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: tipo_gestao
      description: Gestão de saúde
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: esfera_administrativa
      description: Código da esfera administrativa
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: retencao_tributos
      description: Código de retenção de tributos
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: atividade_ensino_pesquisa
      description: Código da atividade de ensino/pesquisa
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: natureza_administrativa
      description: Código da natureza da organização
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: fluxo_atendimento
      description: Código de FLUXO da clientela
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: tipo_unidade
      description: Tipo de unidade (estabelecimento)
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: turno
      description: Código de turno de atendimento
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: nivel_hierarquia
      description: Código do nível de hierarquia
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: tipo_prestador
      description: Tipo de Prestador
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: codigo_banco
      description: Código do Banco do Estabelecimento
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: codigo_agencia
      description: Código da agência bancária do Estabelecimento
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: conta_corrente
      description: Código da Conta Corrente do Estabelecimento
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: contrato_municipio_sus
      description: Número do contrato/convênio MUNICIPAL do vínculo com o SUS
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: data_publicacao_contrato_municipal
      description: Ano e Mês de publicação do contrato/convênio MUNICIPAL
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: contrato_estado_sus
      description: Número do contrato/convênio ESTADUAL do vínculo com o SUS
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: data_publicacao_contrato_estadual
      description: Ano e Mês de publicação do contrato /convênio ESTADUAL
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: numero_alvara
      description: Número do alvará
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: data_expedicao_alvara
      description: Ano e Mês de expedição do alvará
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: orgao_expedidor
      description: Órgão expedidor do alvará
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: avaliacao_acreditacao_hospitalar
      description: Indica se o estabelecimento foi avaliado segundo o Manual de Acreditação Hospitalar do MS
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: classificacao_acreditacao_hospitalar
      description: Classificação da avaliação do estabelecimento segundo o Manual de Acreditação Hospitalar do MS
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: data_acreditacao_ano
      description: Ano da Acreditação
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: data_acreditacao_mes
      description: Mês da Acreditação
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: avaliacao_pnass
      description: Indica se o estabelecimento foi avaliado segundo o Programa Nacional de Serviços de Saúde - PNASS
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: data_avaliacao_pnass_ano
      description: Ano da Avaliação PNASS
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: data_avaliacao_pnass_mes
      description: Mês da Avaliação PNASS
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: gestao_basica_amb_estadual
      description: Indicador se a gestão da atividade ATENÇÃO BÁSICA do Nível de Atenção AMBULATORIAL é ESTADUAL
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: gestao_basica_amb_municipal
      description: Indicador se a gestão da atividade ATENÇÃO BÁSICA do Nível de Atenção AMBULATORIAL é MUNICIPAL
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: gestao_media_amb_estadual
      description: Indicador se a gestão da atividade MÉDIA COMPLEXIDADE do Nível de Atenção AMBULATORIAL é ESTADUAL
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: gestao_media_amb_municipal
      description: Indicador se a gestão da atividade MÉDIA COMPLEXIDADE do Nível de Atenção AMBULATORIAL é MUNICIPAL
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: gestao_alta_amb_estadual
      description: Indicador se a gestão da atividade ALTA COMPLEXIDADE do Nível de Atenção AMBULATORIAL é ESTADUAL
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: gestao_alta_amb_municipal
      description: Indicador se a gestão da atividade ALTA COMPLEXIDADE do Nível de Atenção AMBULATORIAL é MUNICIPAL
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: atencao_amb
      description: Indica a existência de Nível de Atenção AMBULATORIAL, de gestão Municipal/Estadual, para este CNES
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: gestao_estadual
      description: Indicador se a gestão do programa cód. 03 é ESTADUAL
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: gestao_municipal
      description: Indicador se a gestão do programa cód. 03 é MUNICIPAL
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: gestao_media_hosp_estadual
      description: Indicador se a gestão da atividade MÉDIA COMPLEXIDADE do Nível de Atenção HOSPITALAR é ESTADUAL
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: gestao_media_hosp_municipal
      description: Indicador se a gestão da atividade MÉDIA COMPLEXIDADE do Nível de Atenção HOSPITALAR é MUNICIPAL
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: gestao_alta_hosp_estadual
      description: Indicador se a gestão da atividade ALTA COMPLEXIDADE do Nível de Atenção HOSPITALAR é ESTADUAL
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: gestao_alta_hosp_municipal
      description: Indicador se a gestão da atividade ALTA COMPLEXIDADE do Nível de Atenção HOSPITALAR é MUNICIPAL
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: atencao_hosp
      description: Indica a existência de Nível de Atenção HOSPITALAR, de gestão Municipal/Estadual, para este CNES
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: quantidade_leitos_cirurgico
      description: Quantidade de leitos tipo 1 (cirúrgico) existentes
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: quantidade_leitos_clinico
      description: Quantidade de leitos tipo 2 (clínico) existentes
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: quantidade_leitos_complementar
      description: Quantidade de leitos tipo 3 (complem.) existentes
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: indicador_leitos_hosp
      description: Indica a existência de LEITOS HOSPITALARES-CIRPURGICOS, CLÍNICOS E COMPLEMENTARES para este CNES
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: quantidade_consultorio_pediatrico
      description: Quantidade de salas/consultórios de atendimento pediátrico (URGÊNCIA/EMERGÊNCIA)
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: quantidade_consultorio_feminino
      description: Quantidade de salas/consultórios de atendimento feminino (URGÊNCIA/EMERGÊNCIA)
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: quantidade_consultorio_masculino
      description: Quantidade de salas/consultórios de atendimento masculino (URGÊNCIA/EMERGÊNCIA)
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: quantidade_consultorio_indiferenciado
      description: Quantidade de salas/consultórios de atendimento indiferenciado(URGÊNCIA/EMERGÊNCIA)
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: quantidade_sala_repouso_pediatrico_urgencia
      description: Quantidade de salas de repouso/observação pediátrico (URGÊNCIA/EMERGÊNCIA)
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: quantidade_sala_repouso_feminino_urgencia
      description: Quantidade de salas de repouso/observação feminino (URGÊNCIA/EMERGÊNCIA)
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: quantidade_sala_repouso_masculino_urgencia
      description: Quantidade de salas de repouso/observação masculino (URGÊNCIA/EMERGÊNCIA)
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: quantidade_sala_repouso_indiferenciado_urgencia
      description: Quantidade de salas de repouso/observação indiferenciado (URGÊNCIA/EMERGÊNCIA)
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: quantidade_consultorio_odontologia_urgencia
      description: Quantidade de consultórios de odontologia (URGÊNCIA/EMERGÊNCIA)
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: quantidade_sala_higienizacao_urgencia
      description: Quantidade de salas de Higienização (URGÊNCIA/EMERGÊNCIA)
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: quantidade_sala_gesso_urgencia
      description: Quantidade de salas de gesso (URGÊNCIA/EMERGÊNCIA)
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: quantidade_sala_curativo_urgencia
      description: Quantidade de salas de curativos (URGÊNCIA/EMERGÊNCIA)
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: quantidade_sala_pequena_cirurgia_urgencia
      description: Quantidade de salas de pequenas cirurgias (URGÊNCIA/EMERGÊNCIA)
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: quantidade_consultorio_medico_urgencia
      description: Quantidade de consultórios médicos (URGÊNCIA/EMERGÊNCIA)
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: indicador_instalacao_urgencia
      description: Indica a existência de INSTALAÇÃO FÍSICA de URGÊNCIA/EMERGÊNCIA para este CNES
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: quantidade_consultorio_clinica_basica
      description: Quantidade de consultórios de clínica básica (ATEND. AMBULATORIAL)
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: quantidade_consultorio_clinica_especializada
      description: Quantidade de consultórios de clínica especializada (ATEND. AMBULATORIAL)
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: quantidade_consultorio_clinica_indiferenciada
      description: Quantidade de consultórios de clínica indiferenciada (ATEND. AMBULATORIAL)
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: quantidade_consultorio_nao_medico
      description: Quantidade de consultórios não médicos (ATEND. AMBULATORIAL)
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: quantidade_sala_repouso_feminino_amb
      description: Quantidade de salas de repouso/observação feminino (ATEND. AMBULATORIAL)
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: quantidade_sala_repouso_masculino_amb
      description: Quantidade de salas de repouso/observação masculino (ATEND. AMBULATORIAL)
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: quantidade_sala_repouso_pediatrico_amb
      description: Quantidade de salas de repouso/observação pediátrico (ATEND. AMBULATORIAL)
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: quantidade_sala_repouso_indiferenciado_amb
      description: Quantidade de salas de repouso/observação indiferenciado (ATEND. AMBULATORIAL)
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: quantidade_consultorio_odontologia_amb
      description: Quantidade de consultórios de odontologia (ATEND. AMBULATORIAL)
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: quantidade_sala_pequena_cirurgia_amb
      description: Quantidade de salas de pequenas cirurgias (ATEND. AMBULATORIAL)
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: quantidade_sala_enfermagem_amb
      description: Quantidade de salas de enfermagem (ATEND. AMBULATORIAL)
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: quantidade_sala_imunizacao_amb
      description: Quantidade de salas de Imunização (ATEND. AMBULATORIAL)
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: quantidade_sala_nebulizacao_amb
      description: Quantidade de salas de nebulização (ATEND. AMBULATORIAL)
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: quantidade_sala_gesso_amb
      description: Quantidade de salas de gesso (ATEND. AMBULATORIAL)
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: quantidade_sala_curativo_amb
      description: Quantidade de salas de curativos (ATEND. AMBULATORIAL)
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: quantidade_sala_cirurgia_amb
      description: Quantidade de salas de cirurgia ambulatorial (ATEND. AMBULATORIAL)
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: indicador_instalcao_ambulatorial
      description: Indica a existência de INSTALAÇÃO FÍSICA de ATENDIMENTO AMBULATORIAL para este CNES
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: quantidade_sala_cirurgia
      description: Quantidade de salas de cirurgias (CENTRO CIRÚRGICO)
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: quantidade_sala_recuperacao
      description: Quantidade de salas de recuperação (CENTRO CIRÚRGICO)
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: quantidade_sala_cirurgia_amb_centro_cirurgico
      description: Quantidade de salas de cirurgia ambulatorial (CENTRO CIRÚRGICO)
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: indicador_instalacao_centro_cirurgico
      description: Indica a existência de INSTALAÇÃO FÍSICA de ATENDIMENTO HOSPITALAR-CENTRO CIRÚRGICO para este CNES
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: quantidade_sala_pre_parto
      description: Quantidade de salas de pré parto (CENTRO OBSTÉTRICO)
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: quantidade_sala_parto_normal
      description: Quantidade de salas de parto normal (CENTRO OBSTÉTRICO)
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: quantidade_sala_curetagem
      description: Quantidade de salas de curetagem (CENTRO OBSTÉTRICO)
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: quantidade_sala_cirurgia_centro_obstetrico
      description: Quantidade de salas de cirurgias (CENTRO OBSTÉTRICO)
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: indicador_instalacao_centro_obstetrico
      description: Indica a existência de INSTALAÇÃO FÍSICA de ATENDIMENTO HOSPITALAR-CENTRO OBSTÉTRICO para este CNES
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: quantidade_leito_repouso_pediatrico_urgencia
      description: Quantidade de leitos de repouso/observação pediátrico (URGÊNCIA/EMERGÊNCIA)
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: quantidade_leito_repouso_feminino_urgencia
      description: Quantidade de leitos de repouso/observação feminino (URGÊNCIA/EMERGÊNCIA)
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: quantidade_leito_repouso_masculino_urgencia
      description: Quantidade de leitos de repouso/observação masculino (URGÊNCIA/EMERGÊNCIA)
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: quantidade_leito_repouso_indiferenciado_urgencia
      description: Quantidade de leitos de repouso/observação indiferenciado (URGÊNCIA/EMERGÊNCIA)
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: quantidade_equipos_odontologia_urgencia
      description: Quantidade de equipos de odontologia (URGÊNCIA/EMERGÊNCIA)
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: quantidade_leito_repouso_feminino_amb
      description: Quantidade de leitos de repouso/observação feminino (ATEND. AMBULATORIAL)
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: quantidade_leito_repouso_masculino_amb
      description: Quantidade de leitos de repouso/observação masculino (ATEND. AMBULATORIAL)
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: quantidade_leito_repouso_pediatrico_amb
      description: Quantidade de leitos de repouso/observação pediátrico (ATEND. AMBULATORIAL)
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: quantidade_leito_repouso_indiferenciado_amb
      description: Quantidade de leitos de repouso/observação indiferenciado (ATEND.AMBULATORIAL)
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: quantidade_equipos_odontologia_amb
      description: Quantidade de equipos de odontologia (ATEND. AMBULATORIAL)
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: quantidade_leito_recuperacao_centro_cirurgico
      description: Quantidade de leitos de recuperação (CENTRO CIRÚRGICO)
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: quantidade_leito_pre_parto
      description: Quantidade de leitos de pré parto (CENTRO OBSTÉTRICO)
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: quantidade_leito_recem_nascido_normal
      description: Quantidade de leitos de recém-nascido normal (UNID NEONATAL)
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: quantidade_leito_recem_nascido_patologico
      description: Quantidade de leitos de recém-nascido patológico (UNID NEONATAL)
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: quantidade_leito_conjunto_neonatal
      description: Quantidade de leitos de alojamento conjunto (UNID NEONATAL)
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: indicador_instalacao_neonatal
      description: Indica a existência de INSTALAÇÃO FÍSICA de ATENDIMENTO HOSPITALAR- UNIDADE NEONATAL para este CNES
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: indicador_instalaco_hospitalar
      description: Indica a existência de INSTALAÇÃO FÍSICA de ATENDIMENTO HOSPITALAR para este CNES
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: servico_apoio_proprio
      description: Serviço de apoio S.A.M.E. ou S.P.P. próprio
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: servico_apoio_terceirizado
      description: Serviço de apoio S.A.M.E. ou S.P.P.  terceirizado
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: servico_social_proprio
      description: Serviço de apoio Serviço Social próprio
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: servico_social_terceirizado
      description: Serviço de apoio Serviço Social terceirizado
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: servico_farmacia_proprio
      description: Serviço de apoio Farmácia próprio,
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: servico_farmacia_terceirizado
      description: Serviço de apoio Farmácia terceirizado
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: servico_esterilizacao_proprio
      description: Serviço de apoio Esterilização de Materiais próprio
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: servico_esterilizacao_terceirizado
      description: Serviço de apoio Esterilização de Materiais terceirizado
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: servico_nutricao_proprio
      description: Serviço de apoio Nutrição/Dietética (S.N.D.) próprio
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: servico_nutricao_terceirizado
      description: Serviço de apoio Nutrição/Dietética (S.N.D.) terceirizado
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: servico_lactario_proprio
      description: Serviço de apoio Lactário próprio
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: servico_lactario_terceirizado
      description: Serviço de apoio Lactário terceirizado
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: servico_banco_leite_proprio
      description: Serviço de apoio Banco De Leite próprio
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: servico_banco_leite_terceirizado
      description: Serviço de apoio Banco De Leite terceirizado
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: servico_lavanderia_proprio
      description: Serviço de apoio Lavanderia próprio
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: servico_lavanderia_terceirizado
      description: Serviço de apoio Lavanderia terceirizado
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: servico_manutencao_proprio
      description: Serviço de apoio Manutenção de Equipamento próprio
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: servico_manutencao_terceirizado
      description: Serviço de apoio Manutenção de Equipamento terceirizado
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: servico_ambulancia_proprio
      description: Serviço de apoio Ambulância próprio
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: servico_ambulancia_terceirizado
      description: Serviço de apoio Ambulância terceirizado
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: servico_necroterio_proprio
      description: Serviço de apoio Necrotério próprio
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: servico_necroterio_terceirizado
      description: Serviço de apoio Necrotério terceirizado
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: indicador_servico_apoio
      description: Indica a existência de algum serviço de apoio para este CNES
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: coleta_residuo_biologico
      description: Existe coleta de resíduo biológico
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: coleta_residuo_quimico
      description: Existe coleta de resíduo químico
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: coleta_rejeito_radioativo
      description: Existe coleta de rejeitos radioativos
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: coleta_rejeito_comum
      description: Existe coleta de rejeitos comum
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: indicador_coleta_residuo
      description: Indica a existência de alguma coleta de resíduo para este CNES
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: comissao_etica_medica
      description: Existe comissão de ETICA MEDICA
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: comissao_etica_enfermagem
      description: Existe comissão de ETICA DE ENFERMAGEM
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: comissao_farmacia_terapeutica
      description: Existe comissão de FARMÁCIA E TERAPÊUTICA
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: comissao_controle_infeccao
      description: Existe comissão de CONTROLE DE INFECÇÃO HOSPITALAR
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: comissao_apropriacao_custos
      description: Existe comissão de APROPRIAÇÃO DE CUSTOS
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: comissao_cipa
      description: Existe comissão de CIPA
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: comissao_revisao_prontuario
      description: Existe comissão de REVISÃO DE PRONTUÁRIOS
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: comissao_revisao_documentacao
      description: Existe comissão de REVISAO DE DOCUMENTACAO MEDICA E ESTATISTICA
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: comissao_analise_obito_biopisias
      description: Existe comissão de ANALISE DE OBITOS E BIOPISIAS
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: comissao_investigacao_epidemiologica
      description: Existe comissão de INVESTIGACAO EPIDEMIOLOGICA
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: comissao_notificacao_doencas
      description: Existe comissão de NOTIFICACAO DE DOENCAS
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: comissao_zoonose_vetores
      description: Existe comissão de CONTROLE DE ZOONOSES E VETORES
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: indicador_comissao
      description: Indica a existência de alguma comissão para este CNES
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: atendimento_internacao_sus
      description: Atendimento prestado INTERNAÇÃO/Convenio SUS
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: atendimento_internacao_particular
      description: Atendimento prestado INTERNAÇÃO/Convenio PARTICULAR
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: atendimento_internacao_plano_seguro_proprio
      description: Atendimento prestado INTERNAÇÃO/Convenio PLANO-SEGURO PROPRIO
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: atendimento_internacao_plano_seguro_terceiro
      description: Atendimento prestado INTERNAÇÃO/Convenio PLANO-SEGURO TERCEIRO
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: atendimento_internacao_plano_saude_publico
      description: Atendimento prestado INTERNAÇÃO/ Convenio PLANO de SAUDE PUBLICO
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: atendimento_internacao_plano_saude_privado
      description: Atendimento prestado INTERNAÇÃO/Convenio PLANO de SAUDE PRIVADO
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: atendimento_amb_sus
      description: Atendimento prestado ATENDIMENTO AMBULATORIAL/Convenio SUS
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: atendimento_amb_particular
      description: Atendimento prestado ATENDIMENTO AMBULATORIAL/Convenio PARTICULAR
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: atendimento_amb_plano_seguro_proprio
      description: Atendimento prestado ATENDIMENTO AMBULATORIAL/Convenio PLANO- SEGURO PROPRIO
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: atendimento_amb_plano_seguro_terceiro
      description: Atendimento prestado ATENDIMENTO AMBULATORIAL/Convenio PLANO- SEGURO TERCEIRO
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: atendimento_amb_plano_saude_publico
      description: Atendimento prestado ATENDIMENTO AMBULATORIAL/Convenio PLANO de SAUDE PUBLICO
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: atendimento_amb_plano_saude_privado
      description: Atendimento prestado ATENDIMENTO AMBULATORIAL/Convenio PLANO de SAUDE PRIVADO
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: atendimento_sadt_sus
      description: Atendimento prestado SADT/ Convenio SUS
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: atendimento_sadt_privado
      description: Atendimento prestado SADT/ Convenio PARTICULAR
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: atendimento_sadt_plano_seguro_proprio
      description: Atendimento prestado SADT/ Convenio PLANO-SEGURO PROPRIO
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: atendimento_sadt_plano_seguro_terceiro
      description: Atendimento prestado SADT/ Convenio PLANO-SEGURO TERCEIRO
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: atendimento_sadt_plano_saude_publico
      description: Atendimento prestado SADT/ Convenio PLANO de SAUDE PUBLICO
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: atendimento_sadt_plano_saude_privado
      description: Atendimento prestado SADT/ Convenio PLANO de SAUDE PRIVADO
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: atendimento_urgencia_sus
      description: Atendimento prestado URGENCIA/ Convenio SUS
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: atendimento_urgencia_privado
      description: Atendimento prestado URGENCIA/ Convenio PARTICULAR
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: atendimento_urgencia_plano_seguro_proprio
      description: Atendimento prestado URGENCIA/ Convenio PLANO-SEGURO PROPRIO
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: atendimento_urgencia_plano_seguro_terceiro
      description: Atendimento prestado URGENCIA/ Convenio PLANO-SEGURO TERCEIRO
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: atendimento_urgencia_plano_saude_publico
      description: Atendimento prestado URGENCIA/ Convenio PLANO de SAUDE PUBLICO
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: atendimento_urgencia_plano_saude_privado
      description: Atendimento prestado URGENCIA/Convenio PLANO de SAUDE PRIVADO
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: atendimento_outros_sus
      description: Atendimento prestado OUTROS/ Convenio SUS
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: atendimento_outros_privado
      description: Atendimento prestado OUTROS/Convenio PARTICULAR
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: atendimento_outros_plano_seguro_proprio
      description: Atendimento prestado OUTROS/Convenio PLANO-SEGURO PROPRIO
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: atendimento_outros_plano_seguro_terceiro
      description: Atendimento prestado OUTROS/Convenio PLANO-SEGURO TERCEIRO
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: atendimento_outros_plano_saude_publico
      description: Atendimento prestado OUTROS/Convenio PLANO de SAUDE PUBLICO
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: atendimento_outros_plano_saude_privado
      description: Atendimento prestado OUTROS/Convenio PLANO de SAUDE PRIVADO
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: atendimento_vigilancia_sus
      description: Atendimento prestado VIGILANCIA EM SAUDE/Convenio SUS
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: atendimento_vigilancia_privado
      description: Atendimento prestado VIGILANCIA EM SAUDE/Convenio PARTICULAR
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: atendimento_vigilancia_plano_seguro_proprio
      description: Atendimento prestado VIGILANCIA EM SAUDE/Convenio PLANO-SEGURO PROPRIO
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: atendimento_vigilancia_plano_seguro_terceiro
      description: Atendimento prestado VIGILANCIA EM SAUDE/Convenio PLANO-SEGURO TERCEIRO
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: atendimento_vigilancia_plano_saude_publico
      description: Atendimento prestado VIGILANCIA EM SAUDE/Convenio PLANO de SAUDE PUBLICO
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: atendimento_vigilancia_plano_saude_privado
      description: Atendimento prestado VIGILANCIA EM SAUDE/Convenio PLANO de SAUDE PRIVADO
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: atendimento_regulacao_sus
      description: Atendimento prestado REGULACAO/Convenio SUS
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: atendimento_regulacao_privado
      description: Atendimento prestado REGULACAO/Convenio PARTICULAR
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: atendimento_regulacao_plano_seguro_proprio
      description: Atendimento prestado REGULACAO/Convenio PLANO-SEGURO PROPRIO
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: atendimento_regulacao_plano_seguro_terceiro
      description: Atendimento prestado REGULACAO/Convenio PLANO-SEGURO TERCEIRO
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: atendimento_regulacao_plano_saude_publico
      description: Atendimento prestado REGULACAO/Convenio PLANO de SAUDE PUBLICO
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: atendimento_regulacao_plano_saude_privado
      description: Atendimento prestado REGULACAO/Convenio PLANO de SAUDE PRIVADO
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: indicador_atendimento_prestado
      description: Indica a existência de algum atendimento prestado para este CNES
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: data_atualizacao_ano
      description: Ano de competência da atualização da informação pelo estabelecimento
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: data_atualizacao_mes
      description: Mês de competência da atualização da informação pelo estabelecimento
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: data_competencia_ano
      description: Ano de competência da informação
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: data_competencia_mes
      description: Mês de competência da informação
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
    - name: natureza_juridica
      description: Natureza Jurídica
      is_in_staging: true   # Bool [True, False], whether the column is in the staging table
      is_partition: false   # Bool [True, False], whether the column is a partition.
