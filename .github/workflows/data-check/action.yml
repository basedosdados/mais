name: "Data Check"
description: "Checks data quality"
author: "Base dos Dados"
runs:
  using: composite
  steps:
    - run: pip install basedosdados==1.5.2 pytest toml
      shell: bash
    - run: pytest ${{ github.action_path }} > ${{ github.workspace }}/out
      shell: bash
