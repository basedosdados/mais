name: "Data Check"
description: "Checks data quality"
author: "Base dos Dados"
runs:
  using: composite
  steps:
    - run: pip install basedosdados==1.5.2 pyarrow pytest toml
      shell: bash
    - run: pytest -v ${{ github.action_path }}
      shell: bash
