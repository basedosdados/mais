name: "Update Contributors"
description: "Update perimission levels based on contribution"
author: "Base dos Dados"

runs: 
  using: composite
  steps:
    - run: pip install -U emoji PyGithub
      shell: bash
    - run: python ${{ github.action_path }}/generate_markdown.py && python ${{ github.action_path }}/update-collaborators.py
      shell: bash